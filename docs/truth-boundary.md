# Evidence Boundary

## What A Cell Identity Establishes

- the canonical manifest bytes match the Cell ID;
- declared chunks match their content addresses and Merkle roots;
- the `.pha` artifact binds the declared Cell inputs;
- Rootprint lineage and replay match the committed branch state;
- a Memory Capsule preserves its declared local verification state.

## What It Does Not Establish

- that a sensor observed the physical world honestly;
- that a witness is independent;
- that a human or AI interpretation is correct;
- that a stale observation remains current;
- that an unknown region is empty;
- that the deterministic reference Origin represents a real place.
- that a signer is independent merely because a key is distinct;
- that encrypted data is safe after an authorized endpoint decrypts it.

SLBIT text and visual hints are removable, non-core meaning. A semantic mutation
must fail semantic integrity without changing Cell or PHA identity.

Witness receipts state exactly what a signer attested. They remain non-core and
cannot promote physical observation into Power House proof truth.
